# prh exclusion tests

worker は通常の地の文で検出される。

[worker guide](https://example.test/worker-setup) を読む。

> worker は retry する。

**worker** と *retry* を強調する。

background-worker と mop-up-retry は識別子。

ワーカーworker という並びもある。

commit と deploy は地の文でも検出される。
